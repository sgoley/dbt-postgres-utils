name: 'postgres_utils'
config-version: 2
version: '0.0.2'

require-dbt-version: ">=0.17.0"

model-paths: ["models"]
target-path: "target"
clean-targets: ["target", "dbt_modules"]
test-paths: ["test"]
macro-paths: ["macros"]
log-path: "logs"