name: 'postgres_utils'
config-version: 2
version: '1.0.0'

require-dbt-version: ">=0.17.0"

model-paths: ["models"]
target-path: "target"
clean-targets: ["target", "dbt_modules", "dbt_packages"]
test-paths: ["tests"]
macro-paths: ["macros"]
log-path: "logs"
